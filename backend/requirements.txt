# Core
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# Data processing
pandas>=2.2

# HTTP client for flight providers
httpx>=0.27

# Database
SQLAlchemy>=2.0.36
asyncpg>=0.30          # PostgreSQL driver (production)
aiosqlite>=0.20        # SQLite driver (zero-install local fallback)
alembic>=1.14          # schema migrations

# Multi-worker progress streaming. Only used when REDIS_URL is set; with one
# worker the in-process event bus is correct and this is dormant.
redis>=5.2

# Dev / test
pytest>=8.3
pytest-asyncio>=0.24
